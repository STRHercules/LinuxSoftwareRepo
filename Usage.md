# Boostrap Usage

```
nano bootstrap.sh
# paste script, save
chmod +x bootstrap.sh
./bootstrap.sh
```
Confirm the script worked;
```
echo $SHELL
zsh --version
starship --version
nvm --version
node -v
npm -v
```